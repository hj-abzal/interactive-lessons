import {PlotPoint} from '@/components/line-plot/line-plot-scene';

export type MembersPopulationPercent = {
    first: number,
    second: number,
};

export type SimulationPoints = {
    first: PlotPoint[],
    second: PlotPoint[],
};
